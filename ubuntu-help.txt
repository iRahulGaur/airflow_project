below are the commands we need to run in EC2 ubuntu instance

1. sudo apt-get update
2. sudo apt install python3-pip
2. sudo apt install pipx
3. sudo pipx install apache-airflow
4. sudo pipx install pandas 
5. sudo pipx install s3fs

---

airflow standalone